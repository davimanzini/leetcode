#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    string maximumXor(string s, string t) { //MAXIMUM BITWISE XOR AFTER REARRENGEMENT
        
        int size = s.length();

        unordered_set<int> on;

        int first = stoi(s);

    }
};